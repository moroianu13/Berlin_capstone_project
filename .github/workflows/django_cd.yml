name: Django CI/CD (EC2 - DISABLED)

# This workflow is disabled because deployment is now handled by Fly.io
# To re-enable EC2 deployment, uncomment the 'on:' section below and configure secrets

on:
  workflow_dispatch:  # Manual trigger only

# Disabled automatic triggers:
# on:
#   workflow_run:
#     workflows: ["Django CI"]
#     types:
#       - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: false  # Workflow completely disabled
    steps:
      - name: Deployment Disabled
        run: |
          echo "This workflow is disabled. EC2 deployment is no longer active."
          echo "To deploy, use Fly.io or manually configure EC2 deployment."
